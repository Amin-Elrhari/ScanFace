import 'package:flutter/material.dart';


import 'SelectScanner.dart';
/*
class FirstScreen extends StatefulWidget {
  const FirstScreen({Key? key}) : super(key: key);

  @override
  State<FirstScreen> createState() => _FirstScreenState();
}

class _FirstScreenState extends State<FirstScreen> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(



      backgroundColor: Colors.transparent,

      body: Container(

        decoration: BoxDecoration(
            gradient: LinearGradient(
                colors: [Colors.indigo, Colors.blueAccent])),

        child: Padding(
          padding: const EdgeInsets.all(40.0),
          child: Center(
            child: Column(
              children: [
                Image(
                  image: AssetImage("assets/image3.png"),
                  height: 200.0,),

                SizedBox(height: 70,),

                Text("FaceId",style: TextStyle(color: Colors.cyan,fontSize: 50,fontWeight:FontWeight.bold),),
                SizedBox(height: 40,),
                Container(
                  width: 200,
                    child:
                Center(
                  child: Text("ScanFace est une application permet de detecter les visages et donnee les informations ",
                    style: TextStyle(color: Colors.white,fontSize: 10,fontWeight:FontWeight.normal),),
                )),

                SizedBox(height: 90,),
                Container(
                  height: 44.0,
                  decoration: BoxDecoration(
                      gradient: LinearGradient(
                          colors: [Colors.lightBlue, Colors.cyanAccent])),
                  child: ElevatedButton(
                      style: ElevatedButton.styleFrom(
                          backgroundColor: Colors.transparent,



                      ),
                      onPressed: (){
                        Navigator.of(context)
                            .push(MaterialPageRoute(builder: (context) {
                          return ScondScanner();
                        }));
                  },
                      child: Text('Get Started',style: TextStyle(color: Colors.white,
                  fontSize: 20),)),
                ),

              ],
            ),
          ),
        ),
      ),

    );
  }
}
*/